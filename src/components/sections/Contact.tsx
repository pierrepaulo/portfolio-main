import { Container } from "../common/Container";
import { SectionHeader } from "../common/SectionHeader";

export function Contact() {
  return (
    <section id="contato">
      <Container>
        <SectionHeader
          title="Entre em contato"
          subtitle="Vamos alinhar expectativas, stack e próximos passos."
        />
        <div>Contato</div>
      </Container>
    </section>
  );
}
